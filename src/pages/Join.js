import React from "react";

export default function Join() {
  return (
    <section className="page">
      <div className="card">
        <h2>Join the Project</h2>
        <p>
          Want to contribute? Clone the repo, open issues or submit PRs. This demo uses:
        </p>
        <ul>
          <li>React + react-router</li>
          <li>jsPDF + qrcode</li>
          <li>Pinata (IPFS) & ethers (MetaMask)</li>
        </ul>
        <p>Links: (add your GitHub / docs here)</p>
      </div>
    </section>
  );
}
